[build]
builder = "nixpacks"

[deploy]
# Starts gunicorn AND the RQ worker; see start-web.sh for why both live in one service.
startCommand = "sh start-web.sh"
healthcheckPath = "/api/health"
healthcheckTimeout = 300
